function something(elm) {
    return elm.appendChild(document.createElement(h1).innerHTML = "H1 element");
}

function here(num) {
    return num  * 4;
}

export function together(elm, num){
    let numm = elm.innerHTML = here(num);
    let elmm = something(elm);

    return elmm;
}